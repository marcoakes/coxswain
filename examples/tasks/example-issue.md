# Add CSV export to the reports page

Users want to download the reports table as CSV. The button belongs beside
the existing "Print" control, and the file should carry the same columns the
table shows, in the same order.

Out of scope: scheduling, emailing, and any format other than CSV.
