"""Coxswain — compile intent into verified outcomes.

v0 is `cox verify`: a standalone evidence compiler that runs a repo's
declared gates and writes a portable evidence bundle. Binding contract:
SPEC_COX_VERIFY_V0.md at the repo root.
"""

__version__ = "0.1.0.dev0"
